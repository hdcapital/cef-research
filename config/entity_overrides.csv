match_type,raw_name,canonical_id,note,verified_by,verified_date
# Manually verified entity mappings that automatic resolution cannot make.
# match_type: alias|split|successor|exclude
# raw_name: name exactly as it appears in an AIC file
# canonical_id: the internal security id it maps to
# Populate only with independently verified mappings; never guess.
